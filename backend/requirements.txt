flask>=3.0.0
flask-cors>=4.0.0
opencv-python>=4.9.0
scikit-learn>=1.3.0
scipy>=1.11.0
Pillow>=10.0.0
numpy>=1.26.0

# Optional — for MP3/FLAC/OGG/M4A voice input (WAV works without it)
# ffmpeg must be installed system-wide for non-WAV audio:
#   Windows: winget install ffmpeg
#   macOS:   brew install ffmpeg
#   Linux:   apt install ffmpeg